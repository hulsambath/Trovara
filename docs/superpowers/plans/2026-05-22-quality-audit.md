# Quality Audit System Implementation Plan

> **For agentic workers:** Execute this plan with the superpowers plugin. Recommended skill: "subagent driven development" (fresh subagent per task). Alternative: "executing plans" (inline batch with checkpoints). Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an 8-dimension quality audit system that runs read-only sub-agents in parallel, aggregates findings into a ranked punch list, and promotes 4 dimensions to reusable slash commands.

**Architecture:** Four reusable sub-agent definitions in `.claude/agents/`, four slash commands wrapping them in `.claude/commands/`, plus an `audit.md` orchestrator with inline prompts for the 4 one-shot dimensions + the perf half of perf-deps. First run produces `docs/superpowers/audits/2026-05-22/` with 10 markdown files.

**Tech Stack:** Claude Code sub-agents, slash commands, Bash for `flutter pub outdated` / `find` / `grep`, no new runtime code.

**Spec:** `docs/superpowers/specs/2026-05-22-quality-audit-design.md`

---

## File Structure

**Created files:**

```
.claude/agents/
  ├─ security-auditor.md            (Task 1)
  ├─ test-gaps-auditor.md           (Task 2)
  ├─ architecture-auditor.md        (Task 3)
  └─ deps-auditor.md                (Task 4)

.claude/commands/
  ├─ audit-security.md              (Task 5)
  ├─ audit-coverage.md              (Task 6)
  ├─ audit-architecture.md          (Task 7)
  └─ audit-deps.md                  (Task 8)
```

**Modified files:**

```
.claude/commands/
  └─ audit.md                       (Task 9) — currently a stub describing intent;
                                      rewritten as orchestrator with inline prompts
                                      for bugs, fraud, bias, neg-correlation, perf.
```

**Generated by first run (Task 11):**

```
docs/superpowers/audits/2026-05-22/
  ├─ 00_report.md
  ├─ 01_security.md … 08_neg_correlation.md
  └─ 09_remediation_plan.md
```

---

## Conventions used in every agent

Every auditor agent emits findings as a markdown table with this exact column header (do not deviate — the orchestrator parses it):

```
| Severity | Category | File:Line | Finding | Recommendation | Effort |
```

- **Severity:** `Critical` · `High` · `Medium` · `Low`
- **Effort:** `S` (<1h) · `M` (1-4h) · `L` (>4h or design needed)
- **File:Line:** required for code findings; use `N/A` for interpretive findings

---

## Task 1: Create `security-auditor` sub-agent

**Files:**
- Create: `.claude/agents/security-auditor.md`

- [ ] **Step 1: Verify `.claude/agents/` exists and the file does not**

Run:
```bash
ls .claude/agents/ && ls .claude/agents/security-auditor.md 2>/dev/null || echo "OK: file does not exist yet"
```
Expected: directory listing shows `style-reviewer.md` and `test-writer.md`; second command prints `OK: file does not exist yet`.

- [ ] **Step 2: Write the agent file**

Create `.claude/agents/security-auditor.md` with this exact content:

````markdown
---
name: security-auditor
description: Read-only security audit of the Trovara Flutter app. Scans for extractable secrets, OAuth scope width, prompt-injection vectors, ownership-check gaps, and unsafe input boundaries flowing into LLM/ObjectBox. Returns a markdown punch list with file:line refs. Never edits code.
tools: Read, Grep, Glob, Bash
model: sonnet
---

You are the **Trovara security auditor**. Your job is to read code and produce a focused punch list of security findings. You do not write or edit code.

## What to load before reviewing

Read in parallel:
- `CLAUDE.md`
- `lib/core/CLAUDE.md`
- `lib/core/services/ai/CLAUDE.md`
- `lib/constants/config_constants.dart`
- `lib/core/di/service_locator.dart`
- `lib/core/services/auth/` (whole dir)
- `lib/core/services/sync/` (whole dir)

## Scan checklist

For each finding emit one row of the output table.

### Secrets handling
- API keys, tokens, or credentials hardcoded anywhere under `lib/` (use `grep -rE "(api[_-]?key|secret|token|password)\s*=\s*['\"]" lib/`)
- `String.fromEnvironment` defaults that fall back to a real key when `--dart-define` is missing
- Keys passed to plain-text logging (`logger.d(apiKey)`, `print(apiKey)`)
- Keys written to ObjectBox, SharedPreferences, or Drive without encryption

### Drive OAuth
- Requested scopes wider than necessary in `google_drive_service.dart` and friends
- Token refresh failures that fall back to silent re-auth without user awareness
- Drive folder access boundaries — can user A's app instance read user B's notes folder?

### Prompt injection (RAG / chat)
- Imported note content (Obsidian / Notion / Storypad adapters) flowing into LLM prompts without sanitization
- User-typed chat input concatenated directly into system prompts in `prompt_builder_service.dart`
- Note titles or tags injected into prompts without escaping
- LLM responses written back to ObjectBox without validation

### Ownership checks
- Repository methods (`getAll()`, `getById()`, `delete()`) that don't filter by current user id
- Anonymous→user-id migration paths in `note.dart` / `chat_thread_entity.dart` that lose the userId field
- ChatThread, ChatMessage, Note queries that don't scope to the signed-in user

### Input boundaries
- Markdown→Quill conversion accepting untrusted HTML/JS embeds
- Import adapters trusting filename, path, or external URLs without validation
- Vector search returning chunks from other users' notes (cross-user leak)

## Output contract

Return exactly this format (no preamble, no summary text — just the table):

```
| Severity | Category | File:Line | Finding | Recommendation | Effort |
|---|---|---|---|---|---|
| Critical | Secrets | lib/constants/config_constants.dart:42 | Default Gemini API key string is hardcoded | Remove default; fail loudly when --dart-define missing | S |
| High | OwnershipCheck | lib/core/repository/implementations/objectbox_note_repository.dart:88 | getAll() returns notes for all userIds | Filter by current userId from auth service | M |
```

Severity: `Critical` (data loss / breach) · `High` (crash / wrong-result) · `Medium` (degraded UX or tech debt with blast radius) · `Low` (style / nit)

Effort: `S` (<1h) · `M` (1-4h) · `L` (>4h or design needed)

If you find zero issues in a category, omit it from the table. Do not pad with "no findings" rows.

## Rules

- Read-only. Never call Edit or Write.
- Cite exact `file:line` — open the file to confirm the line number.
- One row per distinct finding. If the same root cause appears in 3 files, emit 3 rows (orchestrator dedups).
- Don't speculate. If you can't confirm a vulnerability by reading the code, drop it.
````

- [ ] **Step 3: Verify the file exists and parses as agent frontmatter**

Run:
```bash
head -6 .claude/agents/security-auditor.md
```
Expected: `---` line, `name:`, `description:`, `tools:`, `model:`, `---`.

- [ ] **Step 4: Commit**

```bash
git add .claude/agents/security-auditor.md
git commit -m "feat(audit): add security-auditor subagent"
```

---

## Task 2: Create `test-gaps-auditor` sub-agent

**Files:**
- Create: `.claude/agents/test-gaps-auditor.md`

- [ ] **Step 1: Write the agent file**

Create `.claude/agents/test-gaps-auditor.md` with this exact content:

````markdown
---
name: test-gaps-auditor
description: Read-only test coverage gap analysis for Trovara. Maps every service / repository / ViewModel in lib/ to its patrol_test/ counterpart (or absence), ranks gaps by criticality, and emits a "write these tests next" punch list. Never edits code or writes tests.
tools: Read, Grep, Glob, Bash
model: sonnet
---

You are the **Trovara test-gap auditor**. Your job is to map untested code paths and rank them by how badly they need coverage. You do not write tests.

## What to load before reviewing

Read in parallel:
- `CLAUDE.md`
- `patrol_test/CLAUDE.md`
- `docs/PATROL_UNIT_TESTING.md`

## How to map coverage

Run this Bash one-liner to enumerate source vs. test files:

```bash
{
  echo "── SOURCES (services/repositories/ViewModels) ──"
  find lib/core/services lib/core/repository/implementations lib/views -name "*.dart" \
    ! -name "*.g.dart" ! -path "*/widgets/*" \
    | grep -E "(_service|_repository|_view_model)\.dart$" \
    | sort
  echo
  echo "── EXISTING TESTS ──"
  find patrol_test -name "*_test.dart" | sort
} > /tmp/audit_coverage_map.txt
cat /tmp/audit_coverage_map.txt
```

Then for each source file, derive the expected test path (mirror the source under `patrol_test/`) and check whether the file exists.

## Criticality ranking

Rank each missing test by criticality:

- **Critical** — touches money, auth, sync, or data integrity (anything in `sync/`, `auth/`, RAG pipeline, embeddings, ObjectBox repositories that mutate)
- **High** — user-facing core flows (note CRUD, chat, search, import adapters)
- **Medium** — secondary services (theme, mock data, in-app update)
- **Low** — pure formatting / display helpers

## Output contract

Return exactly this format:

```
| Severity | Category | File:Line | Finding | Recommendation | Effort |
|---|---|---|---|---|---|
| Critical | TestGap | lib/core/services/ai/rag_service.dart:1 | No patrol_test coverage for RagService — orchestrates RAG pipeline | Write patrol_test/core/services/ai/rag_service_test.dart covering query happy path, error propagation, retry behavior | L |
| High | TestGap | lib/core/services/chat/chat_source_service.dart:1 | No coverage for ChatSourceService (just merged 4f077ed) | Write patrol_test/core/services/chat/chat_source_service_test.dart per the test-writer agent's template | M |
```

Use `File:Line` = `<source-file-path>:1` (point at the source, not the missing test).

Severity for test gaps maps to criticality above. Effort: `S` (small service, <100 LOC), `M` (medium, 100-300 LOC), `L` (large or complex, >300 LOC).

If a source file already has a test file, do not emit a row — even if coverage inside that file is partial. Partial-coverage analysis is out of scope.

## Rules

- Read-only.
- One row per missing test file.
- Don't suggest tests for `_view.dart` files (these are 3-line widgets per Views_Style_Guide.md) or generated files.
- Don't suggest tests for entities in `lib/models/` (data classes, covered by repository tests).
````

- [ ] **Step 2: Verify the file exists**

Run:
```bash
head -6 .claude/agents/test-gaps-auditor.md
```
Expected: agent frontmatter.

- [ ] **Step 3: Commit**

```bash
git add .claude/agents/test-gaps-auditor.md
git commit -m "feat(audit): add test-gaps-auditor subagent"
```

---

## Task 3: Create `architecture-auditor` sub-agent

**Files:**
- Create: `.claude/agents/architecture-auditor.md`

- [ ] **Step 1: Write the agent file**

Create `.claude/agents/architecture-auditor.md` with this exact content:

````markdown
---
name: architecture-auditor
description: Read-only architecture + style audit of the Trovara codebase. Catches MVVM violations, ServiceLocator bypasses, files over 300 LOC, hardcoded strings/colors/Material icons, and DRY/KISS/SOLID drift. Complements style-reviewer (which is feature-scoped) — this one is whole-codebase. Never edits code.
tools: Read, Grep, Glob, Bash
model: sonnet
---

You are the **Trovara architecture auditor**. Your job is to scan the whole codebase for architectural drift and style violations. You do not write or edit code.

## What to load before reviewing

Read in parallel:
- `CLAUDE.md`
- `lib/views/CLAUDE.md`
- `lib/core/CLAUDE.md`
- `docs/style_guide/Views_Style_Guide.md`
- `docs/style_guide/File_Organization_Rules.md`

## Scan checklist

### File size violations (use the audit-file-sizes one-liner)

Run:
```bash
find lib -name "*.dart" \
  ! -name "*.g.dart" ! -name "*.freezed.dart" ! -name "*.gr.dart" \
  ! -path "*/gen/*" \
  -print0 | while IFS= read -r -d '' f; do
    loc=$(wc -l < "$f" | tr -d ' ')
    limit=300
    case "$f" in lib/models/*) limit=400 ;; esac
    if [ "$loc" -gt "$limit" ]; then
      printf "%5d  (limit %d)  %s\n" "$loc" "$limit" "$f"
    fi
  done | sort -rn
```

Emit one `Medium` row per over-limit file. Recommend the refactor recipe from `File_Organization_Rules.md` (R1-R6) that matches.

### MVVM violations

- Views (`*_view.dart`, `*_content.dart`) that import `package:trovara/core/services/`
- ViewModels that import `package:flutter/material.dart` for Widget types (importing it for `BuildContext` / `Color` is OK)
- ViewModels that call `Navigator.push` directly (should use `context.push`)

Grep helpers:
```bash
grep -rln "package:trovara/core/services" lib/views/
grep -rln "Navigator.push\|Navigator.of(context).push" lib/views/ --include="*_view_model.dart"
```

### ServiceLocator bypasses

- Concrete instantiation of services outside `service_locator.dart`:
  ```bash
  grep -rE "= (RagService|EmbeddingService|ChatService|NoteService|GoogleDriveService|GoogleDriveSyncService)\(" lib/ \
    | grep -v "service_locator.dart"
  ```
- Direct instantiation of ObjectBox repos:
  ```bash
  grep -rE "= ObjectBox[A-Z][A-Za-z]+Repository\(" lib/ | grep -v "service_locator.dart"
  ```

### Hardcoded strings (i18n violations)

- Likely user-visible strings missing `tr()`:
  ```bash
  grep -rEn "Text\(\s*['\"]" lib/views/ lib/widgets/ | grep -v "tr("
  ```
- AppBar/SnackBar/AlertDialog titles with raw strings.

### Hardcoded colors / text styles

- Material `Colors.*` (other than `Colors.transparent`):
  ```bash
  grep -rEn "Colors\.(red|blue|green|orange|yellow|purple|black|white|grey|gray|brown|pink|cyan|lime|teal|indigo)" lib/
  ```
- Inline `TextStyle(fontSize:` instead of `Theme.of(context).textTheme.*`:
  ```bash
  grep -rEn "TextStyle\(\s*fontSize:" lib/
  ```

### Material icons (should be Lucide only)

```bash
grep -rEn "Icons\.[a-z]" lib/ --include="*.dart" | grep -v "lucide_icons"
```

### Print statements (logger only)

```bash
grep -rEn "\bprint\(" lib/ --include="*.dart"
```

## Output contract

Return exactly this format:

```
| Severity | Category | File:Line | Finding | Recommendation | Effort |
|---|---|---|---|---|---|
| Medium | FileSize | lib/views/search/search_content.dart:1 | 875 LOC exceeds 300 limit | Apply Recipe R1: extract widgets/search_filters.dart, widgets/search_results.dart | L |
| High | MVVM | lib/views/chat/chat_content.dart:42 | View imports core/services/chat_service.dart directly | Move dependency into ChatViewModel via ServiceLocator | S |
| Medium | I18n | lib/views/notes/notes_content.dart:88 | Hardcoded 'Create note' string | Move to en.json + km.json and use tr('notes.create') | S |
| Low | Style | lib/widgets/foo.dart:12 | Uses Colors.blue instead of Theme.of(context).colorScheme.primary | Replace with theme color | S |
```

Categories: `MVVM`, `ServiceLocator`, `FileSize`, `I18n`, `Color`, `TextStyle`, `Icon`, `Print`, `DRY`, `KISS`.

Severity rules:
- `MVVM` / `ServiceLocator` bypass = `High` (compounds badly)
- `FileSize` = `Medium` (tech debt; effort is `L` for over-500 LOC, `M` otherwise)
- `I18n` / `Color` / `Icon` / `Print` = `Low` per occurrence, but if a single file has >10 occurrences, emit one `Medium` row pointing at the file

## Rules

- Read-only.
- Don't double-count: if a file is both >300 LOC and has 5 hardcoded colors, emit two rows (one FileSize, one Color rollup) — not 7.
- Skip generated files (`*.g.dart`, `objectbox.g.dart`, `lib/gen/`).
- Skip `lib/objectbox-model.json` and `pubspec.lock`.
````

- [ ] **Step 2: Commit**

```bash
git add .claude/agents/architecture-auditor.md
git commit -m "feat(audit): add architecture-auditor subagent"
```

---

## Task 4: Create `deps-auditor` sub-agent

**Files:**
- Create: `.claude/agents/deps-auditor.md`

- [ ] **Step 1: Write the agent file**

Create `.claude/agents/deps-auditor.md` with this exact content:

````markdown
---
name: deps-auditor
description: Read-only supply-chain audit of pubspec.yaml. Runs `flutter pub outdated`, flags packages stuck on old major versions, checks for known CVEs against pub.dev advisories, and emits a punch list of upgrades to consider. Never modifies pubspec or pubspec.lock.
tools: Read, Grep, Glob, Bash
model: sonnet
---

You are the **Trovara dependency auditor**. Your job is to surface outdated and potentially vulnerable dependencies. You do not edit pubspec.yaml or run pub upgrade.

## What to load before reviewing

Read in parallel:
- `pubspec.yaml`
- `pubspec.lock`

## Scan steps

1. Capture current state:
   ```bash
   flutter pub outdated --no-dev-dependencies 2>&1 | tee /tmp/audit_pub_outdated.txt
   flutter pub outdated --dev-dependencies 2>&1 | tee /tmp/audit_pub_outdated_dev.txt
   ```

2. For each package with a newer resolvable version, emit one row.

3. For each package with a newer major version that is NOT resolvable (constraint blocks it), emit a `Medium` row explaining the upgrade path.

4. Check for known-vulnerable packages by checking the package name against well-known advisories (Flutter SDK changelogs, pub.dev security advisories you know of from training data; do NOT invent CVEs). If you have no confirmed knowledge of a CVE, emit a `Low` row noting "manually verify on pub.dev/<pkg>".

## Output contract

Return exactly this format:

```
| Severity | Category | File:Line | Finding | Recommendation | Effort |
|---|---|---|---|---|---|
| High | DepCVE | pubspec.yaml:42 | http: 0.13.5 has known CVE-2023-XXXX | Upgrade to ^1.1.0 | S |
| Medium | DepOutdated | pubspec.yaml:55 | objectbox: 2.5.0; latest is 4.0.1 (major upgrade) | Review breaking changes in changelog, upgrade in dedicated PR | L |
| Low | DepOutdated | pubspec.yaml:60 | logger: 2.0.2; latest is 2.4.0 (patch+minor) | Bump with `flutter pub upgrade logger` | S |
```

Categories: `DepCVE` (Critical/High), `DepOutdated` (Medium for major lag, Low for patch/minor lag), `DepDeprecated` (Medium — package marked discontinued on pub.dev).

Severity rules:
- Confirmed CVE = `Critical` (if RCE/data-leak) or `High` (other)
- Major version behind = `Medium`
- Patch/minor behind = `Low`
- Package discontinued on pub.dev = `Medium`

## Rules

- Read-only. Never run `flutter pub upgrade`, `flutter pub add`, or edit pubspec files.
- Don't invent CVEs. If you don't have confirmed advisory knowledge for a package, mark it `Low` with "manually verify on pub.dev/<pkg>/versions".
- Don't include `flutter`, `flutter_test`, `cupertino_icons` — the SDK and stub deps churn separately.
````

- [ ] **Step 2: Commit**

```bash
git add .claude/agents/deps-auditor.md
git commit -m "feat(audit): add deps-auditor subagent"
```

---

## Task 5: Create `/audit-security` slash command

**Files:**
- Create: `.claude/commands/audit-security.md`

- [ ] **Step 1: Write the command file**

Create `.claude/commands/audit-security.md` with this exact content:

````markdown
---
description: Run the security-auditor subagent and append findings to the current dated audit folder under docs/superpowers/audits/.
---

# /audit-security

Dispatch the `security-auditor` subagent against the whole `lib/` tree, then save its output to `docs/superpowers/audits/YYYY-MM-DD/01_security.md`.

## Steps

1. Determine today's date in ISO format:
   ```bash
   date +%Y-%m-%d
   ```

2. Ensure the audit folder exists:
   ```bash
   mkdir -p "docs/superpowers/audits/$(date +%Y-%m-%d)"
   ```

3. Dispatch `security-auditor` using the Agent tool with `subagent_type: "security-auditor"`. Prompt:

   > Audit the Trovara codebase for security issues per your standing instructions. Scope: entire `lib/` tree. Return only the markdown table.

4. Write the returned table to `docs/superpowers/audits/<today>/01_security.md`, prefaced with:

   ```markdown
   # Security Audit — <today>

   Generated by `/audit-security`. See `00_report.md` for aggregated cross-dimension view.

   <table from subagent>
   ```

5. Print the count of findings by severity to stdout for the user, e.g.:
   ```
   Security audit saved to docs/superpowers/audits/2026-05-22/01_security.md
   Findings: 2 Critical · 5 High · 3 Medium · 1 Low
   ```

## Notes

- This command is idempotent — running it twice on the same day overwrites `01_security.md`.
- Does not modify any other file in the audit folder.
- For the aggregated report, run `/audit` (orchestrator).
````

- [ ] **Step 2: Commit**

```bash
git add .claude/commands/audit-security.md
git commit -m "feat(audit): add /audit-security slash command"
```

---

## Task 6: Create `/audit-coverage` slash command

**Files:**
- Create: `.claude/commands/audit-coverage.md`

- [ ] **Step 1: Write the command file**

Create `.claude/commands/audit-coverage.md` with this exact content:

````markdown
---
description: Run the test-gaps-auditor subagent and append findings to the current dated audit folder under docs/superpowers/audits/.
---

# /audit-coverage

Dispatch the `test-gaps-auditor` subagent to map untested services / repositories / ViewModels, then save its output to `docs/superpowers/audits/YYYY-MM-DD/03_test_gaps.md`.

## Steps

1. Resolve today's date and ensure the folder exists:
   ```bash
   mkdir -p "docs/superpowers/audits/$(date +%Y-%m-%d)"
   ```

2. Dispatch `test-gaps-auditor` with prompt:

   > Map untested services, repositories, and ViewModels under `lib/` per your standing instructions. Return only the markdown table.

3. Write the returned table to `docs/superpowers/audits/<today>/03_test_gaps.md`, prefaced with:

   ```markdown
   # Test Coverage Gaps — <today>

   Generated by `/audit-coverage`. To write a missing test, invoke the `test-writer` subagent with the source file path.

   <table from subagent>
   ```

4. Print counts by severity to stdout.

## Notes

- Idempotent — overwrites `03_test_gaps.md` for the current day.
- Pair with the `test-writer` subagent to actually generate the missing tests.
````

- [ ] **Step 2: Commit**

```bash
git add .claude/commands/audit-coverage.md
git commit -m "feat(audit): add /audit-coverage slash command"
```

---

## Task 7: Create `/audit-architecture` slash command

**Files:**
- Create: `.claude/commands/audit-architecture.md`

- [ ] **Step 1: Write the command file**

Create `.claude/commands/audit-architecture.md` with this exact content:

````markdown
---
description: Run the architecture-auditor subagent and append findings to the current dated audit folder under docs/superpowers/audits/.
---

# /audit-architecture

Dispatch the `architecture-auditor` subagent against the whole `lib/` tree, then save its output to `docs/superpowers/audits/YYYY-MM-DD/04_architecture.md`.

## Steps

1. Resolve today's date and ensure the folder exists:
   ```bash
   mkdir -p "docs/superpowers/audits/$(date +%Y-%m-%d)"
   ```

2. Dispatch `architecture-auditor` with prompt:

   > Scan the whole `lib/` tree for MVVM violations, ServiceLocator bypasses, file-size violations, and style/i18n drift per your standing instructions. Return only the markdown table.

3. Write the returned table to `docs/superpowers/audits/<today>/04_architecture.md`, prefaced with:

   ```markdown
   # Architecture Audit — <today>

   Generated by `/audit-architecture`. Complements the `style-reviewer` subagent (feature-scoped) — this view is whole-codebase.

   <table from subagent>
   ```

4. Print counts by severity to stdout.

## Notes

- Idempotent — overwrites `04_architecture.md` for the current day.
- Run before opening any PR. For per-feature checks, prefer the `style-reviewer` subagent on the changed files.
````

- [ ] **Step 2: Commit**

```bash
git add .claude/commands/audit-architecture.md
git commit -m "feat(audit): add /audit-architecture slash command"
```

---

## Task 8: Create `/audit-deps` slash command

**Files:**
- Create: `.claude/commands/audit-deps.md`

- [ ] **Step 1: Write the command file**

Create `.claude/commands/audit-deps.md` with this exact content:

````markdown
---
description: Run the deps-auditor subagent and append findings to the current dated audit folder under docs/superpowers/audits/.
---

# /audit-deps

Dispatch the `deps-auditor` subagent to surface outdated and potentially vulnerable pubspec dependencies. Saves output to `docs/superpowers/audits/YYYY-MM-DD/05_perf_deps.md` (deps half only; perf half is `--full`-only via orchestrator).

## Steps

1. Resolve today's date and ensure the folder exists:
   ```bash
   mkdir -p "docs/superpowers/audits/$(date +%Y-%m-%d)"
   ```

2. Dispatch `deps-auditor` with prompt:

   > Audit pubspec dependencies per your standing instructions. Return only the markdown table.

3. Write the returned table to `docs/superpowers/audits/<today>/05_perf_deps.md`, prefaced with:

   ```markdown
   # Dependencies Audit — <today>

   Generated by `/audit-deps`. Perf hotspots are surfaced separately via `/audit --full`.

   <table from subagent>
   ```

4. Print counts by severity to stdout.

## Notes

- Strictly read-only — never runs `flutter pub upgrade` or modifies pubspec files. Open question §6.2 in the spec defaulted to read-only.
- Idempotent — overwrites `05_perf_deps.md` for the current day.
- If `/audit --full` also runs today, the perf half will be appended to this file by the orchestrator.
````

- [ ] **Step 2: Commit**

```bash
git add .claude/commands/audit-deps.md
git commit -m "feat(audit): add /audit-deps slash command"
```

---

## Task 9: Rewrite `/audit` orchestrator

**Files:**
- Modify: `.claude/commands/audit.md` (currently a 40-line stub describing intent)

This is the largest task. The orchestrator contains:
- Default-mode logic (run the 4 promoted commands in parallel)
- `--full` flag logic (also dispatch 5 inline one-shot agents)
- `--only <name>` flag logic
- Inline prompts for bugs, fraud, bias, neg-correlation, and the perf half of perf-deps
- Aggregation pipeline that produces `00_report.md` and `09_remediation_plan.md`

- [ ] **Step 1: Read the current stub to confirm we're overwriting the right thing**

```bash
cat .claude/commands/audit.md
```
Expected: 40-line markdown stub starting with `# /audit` describing intent (no executable steps).

- [ ] **Step 2: Overwrite with the orchestrator**

Replace the entire contents of `.claude/commands/audit.md` with:

````markdown
---
description: Orchestrate a multi-dimension quality audit. Default mode runs the 4 promoted subagents (security, coverage, architecture, deps) in parallel and aggregates into a ranked punch list. --full also dispatches the 5 one-shot dimensions (bugs, fraud, bias, neg-correlation, perf).
---

# /audit — Quality Audit Orchestrator

## Flags

- (none) — default mode: run the 4 promoted commands in parallel
- `--full` — also dispatch 5 inline one-shot agents (bugs, fraud, bias, neg-correlation, perf)
- `--only <name>` — run a single sub-command; valid names: `security`, `coverage`, `architecture`, `deps`, `bugs`, `fraud`, `bias`, `neg-correlation`, `perf`

## Severity / Effort scale

- **Severity:** `Critical` (data loss / breach) · `High` (crash / wrong-result) · `Medium` (degraded UX / tech debt with blast radius) · `Low` (style / nit)
- **Effort:** `S` (<1h) · `M` (1-4h) · `L` (>4h or design needed)

## Steps

### 1. Setup

Resolve today's date and create the audit folder:
```bash
TODAY=$(date +%Y-%m-%d)
mkdir -p "docs/superpowers/audits/$TODAY"
echo "Audit folder: docs/superpowers/audits/$TODAY"
```

### 2. Parse the flag

- If `--only <name>` was passed, jump to step 3 and dispatch only that one agent.
- If `--full` was passed, set `RUN_FULL=true`; default is `RUN_FULL=false`.

### 3. Dispatch promoted commands in parallel

In a single Agent tool batch, dispatch all 4 promoted subagents (NOT slash commands — invoke the subagents directly so we can run in parallel; the slash-command files are for ad-hoc per-dimension runs by humans):

- `security-auditor` — "Audit `lib/` for security issues per your standing instructions. Return only the table."
- `test-gaps-auditor` — "Map untested services/repos/ViewModels per your standing instructions. Return only the table."
- `architecture-auditor` — "Scan `lib/` per your standing instructions. Return only the table."
- `deps-auditor` — "Audit pubspec per your standing instructions. Return only the table."

Save each returned table to the corresponding file:
- `01_security.md`
- `03_test_gaps.md`
- `04_architecture.md`
- `05_perf_deps.md` (deps half — perf will be appended in step 4 if `--full`)

Each file is prefaced with:
```markdown
# <Title> — <date>

Generated by /audit. See `00_report.md` for aggregated view.

<table>
```

### 4. If --full, dispatch the 5 one-shot agents in parallel

In a single Agent tool batch using `subagent_type: "general-purpose"`, dispatch 5 inline agents with the prompts below. Save each table under the appropriate filename.

#### 4a. bugs-crashes-auditor → `02_bugs.md`

Prompt:

> You are the Trovara bugs/crashes auditor. Read-only. Read `CLAUDE.md`, `lib/core/CLAUDE.md`, `lib/core/services/ai/CLAUDE.md` first. Then scan `lib/` for:
>
> 1. `try/catch` blocks that swallow errors silently (no log, no rethrow, no user feedback). Grep: `catch \(.*\)\s*\{\s*\}` and `catch \(.*\)\s*\{\s*//`.
> 2. Unawaited futures: `grep -rEn "(repository|service|client)\.[a-zA-Z]+\(" lib/ | grep -v await` — manually filter for actual misses, not assignment patterns.
> 3. Null-bang `!` on nullable types in business logic: `grep -rEn "\.[a-zA-Z]+\!" lib/core lib/views`.
> 4. Stream subscriptions never cancelled (StreamSubscription stored in field but no `dispose()` or `cancel()` in `dispose`/`tearDown`).
> 5. Race conditions in sync: `lib/core/services/sync/google_drive_sync_service.dart` and `lib/core/services/chat/chat_drive_sync_service.dart` — concurrent reads/writes without locks, mutable shared state.
>
> Return only this table (no preamble):
>
> ```
> | Severity | Category | File:Line | Finding | Recommendation | Effort |
> ```
>
> Categories: `Bug`, `Crash`, `Race`, `Leak`. Severity: `Critical` (data loss), `High` (crash/wrong-result), `Medium` (degraded), `Low` (nit). Effort: `S`/`M`/`L`. Cite exact `file:line`. Don't speculate.

#### 4b. fraud-auditor → `06_fraud.md`

Prompt:

> You are the Trovara fraud/abuse auditor. Read-only. Read `CLAUDE.md`, `lib/core/CLAUDE.md`, `lib/core/services/ai/CLAUDE.md`, `lib/core/services/auth/`, `lib/constants/config_constants.dart`. Then scan for:
>
> 1. **API/cost abuse:** Unbounded RAG calls (RagService.query without a per-session/per-user rate limit), embedding loops in `embedding_service.dart` without backoff, API keys extractable from the APK (any `String.fromEnvironment` default that is a real key).
> 2. **Auth/identity abuse:** Anonymous→user-id migration in `note.dart` and `chat_thread_entity.dart` — paths where notes can lose their userId or be claimed by another user. Drive OAuth tokens stored where another app instance can read them.
> 3. **Content abuse:** Imported notes (Obsidian/Notion/Storypad adapters in `lib/core/import/adapters/`) flowing into LLM prompts unsanitized — prompt-injection payloads inside note bodies. Chat input concatenated into system prompts in `prompt_builder_service.dart`.
> 4. **Data exfiltration:** Vector search in `vector_search_service.dart` returning chunks from other users' notes. Embeddings written without userId scoping. Drive folder access wider than the current user's scope.
>
> Return only the table format above. Categories: `APIAbuse`, `AuthAbuse`, `ContentAbuse`, `Exfil`. Severity: `Critical` for confirmed cross-user leaks; `High` for unbounded cost paths or auth gaps; `Medium` for unsanitized inputs. Effort: `S`/`M`/`L`.

#### 4c. bias-auditor → `07_bias.md`

Prompt:

> You are the Trovara bias auditor. Read-only. Read `CLAUDE.md`, `lib/core/services/ai/CLAUDE.md`, `lib/core/services/ai/vector_search_service.dart`, `lib/core/services/ai/prompt_builder_service.dart`, `lib/core/services/ai/rag_service.dart`, `lib/views/insights/`. Then evaluate:
>
> 1. **RAG retrieval bias:** Does `vector_search_service.dart` favor recent notes (recency in scoring), longer notes (chunk count weighting), or English-only embeddings (km text gets weaker matches)? Read the score computation and weighting code.
> 2. **LLM output bias:** Does `prompt_builder_service.dart` use a system prompt that biases tone or language? Are Khmer queries forced through English-only prompts?
> 3. **Insights sentiment bias:** In `lib/views/insights/`, does the sentiment chart depend on an English-only classifier? Are tag frequencies normalized across languages?
> 4. **UX defaults:** Are user-affecting defaults set in a way that nudges behavior — cloud sync on by default, anonymous mode hidden, etc.? Check `lib/views/setting/` and `lib/initializer.dart`.
>
> Return only the table format above. Most rows will be `File:Line = N/A` interpretive findings (these go into a "Conceptual findings" section in `00_report.md`). Categories: `RagBias`, `LlmBias`, `UxBias`. Severity: `Medium` typical; `High` if a bias actively excludes Khmer users from a feature. Effort: usually `M`/`L`.

#### 4d. neg-correlation-auditor → `08_neg_correlation.md`

Prompt:

> You are the Trovara negative-correlation auditor. Read-only. Read `CLAUDE.md`, `lib/core/services/ai/embedding_service.dart`, `lib/core/services/sync/`, `lib/core/services/chat/chat_drive_sync_service.dart`, `lib/views/trash/`, `lib/views/insights/`. Then look for:
>
> 1. **Features that fight each other:** Embedding cache (in `embedding_service.dart`) invalidation racing with note edits in `note_view_model.dart`. Drive sync overwriting unsynced local edits (compare timestamps in `google_drive_sync_service.dart`). Trash restore breaking embedding linkage (does restoring a note from trash re-embed it, or leave dangling embeddings?).
> 2. **Quality vs. cost trade-offs:** Where does increasing RAG accuracy (top-k, query expansion) directly inflate API cost or latency? Look at `multi_query_expansion_service.dart` and `rag_service.dart`. Note the trade-off without prescribing a value.
> 3. **Insights chart correctness:** Read the sentiment + tag-frequency chart math in `lib/views/insights/`. Does it correctly handle negative correlations (or does it confuse anti-correlation with no correlation)? Are zero-sample buckets handled?
>
> Return only the table format above. Categories: `FeatureConflict`, `Tradeoff`, `MathError`. Most rows will be `N/A` line or specific `file:line` for math errors. Severity: `High` for active data corruption (e.g., sync overwriting edits); `Medium` for trade-offs that should be surfaced to the user; `Low` for math nits. Effort: usually `M`/`L`.

#### 4e. perf-auditor → returned table appended to `05_perf_deps.md` by orchestrator

Prompt:

> You are the Trovara performance auditor. Read-only. Read `CLAUDE.md`, `lib/core/services/ai/embedding_service.dart`, `lib/core/services/ai/embedding_chunker.dart`, `lib/views/search/search_content.dart`, `lib/core/services/notes/note_service.dart`. Then look for:
>
> 1. **Main-isolate work:** Embedding chunking (`embedding_chunker.dart`) running synchronously on the UI isolate. Large markdown→Quill conversion (`markdown_to_quill_converter` if exists) blocking UI.
> 2. **Unbounded list rendering:** `search_content.dart` (875 LOC) — is the result list virtualized (ListView.builder) or fully built?
> 3. **ObjectBox query patterns:** Repository methods that load `getAll()` then filter in Dart (should use ObjectBox query conditions).
> 4. **Memory leaks:** Image cache not capped, large notes held in chat history forever, stream subscriptions accumulating.
>
> Return only the table format above (no preamble). Categories: `MainIsolate`, `ListRender`, `QueryPattern`, `MemLeak`. Severity: `High` for confirmed jank (>16ms work on UI thread); `Medium` for sub-optimal patterns; `Low` for nits. Effort: `S`/`M`/`L`.

After perf-auditor returns, orchestrator appends the table to `05_perf_deps.md` under a new `## Perf findings` heading. The earlier deps findings (from step 3) stay intact above it. Because step 3 (parallel batch of 4 promoted agents) fully completes before step 4 starts, there is no race on `05_perf_deps.md`.

### 5. Aggregate

After all dispatched agents return:

1. Parse each per-dimension file's table into a flat list of finding objects with fields `{severity, category, file, line, finding, recommendation, effort, source_agent}`.

2. **Dedup:** key by `(file, line ±3 lines)`. On collision: keep higher severity, merge `category` (comma-join), merge `recommendation` (semicolon-join), list both source agents in a `Sources:` column.

3. **Rank:** sort by severity weight first, then by ROI score = severity_weight / effort_weight.

   | Severity | Weight | | Effort | Weight |
   |---|---|---|---|---|
   | Critical | 8 | | S | 1 |
   | High | 4 | | M | 3 |
   | Medium | 2 | | L | 8 |
   | Low | 1 | | | |

   Worked: `Critical/S` (8.0) > `Critical/M` (2.67) > `High/S` (4.0) > `High/M` (1.33) > `Medium/S` (2.0).

4. **Write `00_report.md`** with this structure:

   ```markdown
   # Quality Audit Report — <today>

   ## Summary

   | Severity | Count |
   |---|---|
   | Critical | N |
   | High | N |
   | Medium | N |
   | Low | N |
   | **Total** | N |

   ## Top 10 by ROI

   <table of top 10 ranked findings>

   ## Conceptual findings (no file:line)

   <table of all N/A rows, grouped by source agent>

   ## Full ranked findings

   <table of all dedup'd findings, sorted by score>

   ## Per-dimension drill-downs

   - [Security](01_security.md)
   - [Bugs & crashes](02_bugs.md)
   - [Test gaps](03_test_gaps.md)
   - [Architecture](04_architecture.md)
   - [Perf & deps](05_perf_deps.md)
   - [Fraud](06_fraud.md)
   - [Bias](07_bias.md)
   - [Negative correlation](08_neg_correlation.md)

   ## Remediation plan

   See [09_remediation_plan.md](09_remediation_plan.md).
   ```

5. **Write `09_remediation_plan.md`** with proposed PR batches:

   ```markdown
   # Remediation Plan — <today>

   Proposed PR batches, ordered by urgency × tractability.

   ## Batch 1 — Critical / High with S effort (ship this week)

   <table of all Critical+High findings with effort=S>

   PR scope: one PR with all these fixes. Estimated 1-2 days.

   ## Batch 2 — Critical / High with M effort (ship next sprint)

   <table>

   PR scope: one PR per category (security / bugs / architecture). Estimated 1 week.

   ## Batch 3 — Medium with S/M effort

   <table>

   PR scope: bundle by area. Run `style-reviewer` agent on the diff before opening.

   ## Batch 4 — L-effort items (design needed)

   <table>

   These need a design discussion first. Open a GitHub issue per item before starting work. (Spec §6.1 defaults to PR-only; revisit if a batch needs an issue.)

   ## Low-priority backlog

   Remaining `Low` findings. Address opportunistically when touching the area.
   ```

### 6. Report back to user

Print:
```
Audit complete.

Folder: docs/superpowers/audits/<today>/
Files written:
  00_report.md           <N findings, top-10 ranked>
  01_security.md         <count>
  02_bugs.md             <count>          (--full only)
  03_test_gaps.md        <count>
  04_architecture.md     <count>
  05_perf_deps.md        <count>          (perf appended if --full)
  06_fraud.md            <count>          (--full only)
  07_bias.md             <count>          (--full only)
  08_neg_correlation.md  <count>          (--full only)
  09_remediation_plan.md <N batches>

Next: review 09_remediation_plan.md and approve Batch 1.
```

## Notes

- All sub-agents are read-only. The orchestrator only writes to `docs/superpowers/audits/<today>/`.
- Default mode is fast (~5 min, 4 parallel agents). `--full` adds ~10 min for the 5 one-shot agents.
- The `style-reviewer` and `test-writer` subagents are NOT invoked here — they are feature-scoped and require human dispatch. This orchestrator targets whole-codebase drift.
- The audit folder is committed to git so trends are visible over time.
````

- [ ] **Step 3: Confirm the file is well-formed**

```bash
head -3 .claude/commands/audit.md
wc -l .claude/commands/audit.md
```
Expected: starts with `---\ndescription:` frontmatter; line count well above 200 (orchestrator is the biggest file).

- [ ] **Step 4: Commit**

```bash
git add .claude/commands/audit.md
git commit -m "feat(audit): rewrite /audit as multi-dimension orchestrator

Replaces the intent-only stub with a working orchestrator that
dispatches the 4 promoted subagents in parallel by default and 5
inline one-shot agents under --full. Aggregates findings via
severity x effort ROI ranking into 00_report.md and proposes PR
batches in 09_remediation_plan.md."
```

---

## Task 10: Smoke-test each promoted command individually

This task validates the 4 building blocks before running them through the orchestrator. No commit at the end — these runs produce audit artifacts in step 11.

- [ ] **Step 1: Smoke-test `/audit-security`**

Manually trigger the `/audit-security` slash command. Expected output:
- A new file at `docs/superpowers/audits/<today>/01_security.md` exists.
- It contains a markdown table with the documented column header.
- Stdout shows finding counts by severity.

If the file is empty or missing the table header, debug the security-auditor agent prompt and re-run.

- [ ] **Step 2: Smoke-test `/audit-coverage`**

Trigger `/audit-coverage`. Expected: `03_test_gaps.md` exists with the table.

- [ ] **Step 3: Smoke-test `/audit-architecture`**

Trigger `/audit-architecture`. Expected: `04_architecture.md` exists with the table. Sanity-check: it should flag the 6 known files >300 LOC (`rag_service.dart`, `llm_client.dart`, `search_content.dart`, `note_view_model.dart`, `unified_tags_icon_button.dart`, `custom_tags_widget.dart`).

- [ ] **Step 4: Smoke-test `/audit-deps`**

Trigger `/audit-deps`. Expected: `05_perf_deps.md` exists with the table. Sanity-check: at least one row should match the output of `flutter pub outdated`.

- [ ] **Step 5: If all 4 pass, delete the smoke-test artifacts so the full-run output starts clean**

```bash
rm -rf "docs/superpowers/audits/$(date +%Y-%m-%d)"
```
Expected: the folder is gone; the next `/audit --full` run will recreate it.

---

## Task 11: Run `/audit --full` end-to-end

- [ ] **Step 1: Run the orchestrator with the `--full` flag**

Invoke `/audit --full`. This should:
- Dispatch 4 promoted subagents + 5 inline one-shot agents.
- Produce 10 files under `docs/superpowers/audits/2026-05-22/`.
- Print the summary block from Task 9 step 6.

- [ ] **Step 2: Verify all 10 files exist and are non-empty**

```bash
ls -lh docs/superpowers/audits/2026-05-22/
wc -l docs/superpowers/audits/2026-05-22/*.md
```
Expected: 10 files, each >5 lines.

- [ ] **Step 3: Verify `00_report.md` parses**

Open `00_report.md` and confirm:
- Summary table has 4 severity rows + total.
- Top-10 table has up to 10 rows.
- Per-dimension drill-down links resolve.
- Remediation plan link resolves.

- [ ] **Step 4: Commit the audit folder**

```bash
git add docs/superpowers/audits/2026-05-22/
git commit -m "docs(audit): first quality audit run — 2026-05-22"
```

---

## Task 12: User reviews `09_remediation_plan.md` and approves Batch 1

This is a synchronous gate — implementation pauses until the user approves a batch.

- [ ] **Step 1: Surface the remediation plan to the user**

Print:
```
Audit complete. Please review:
  docs/superpowers/audits/2026-05-22/00_report.md     ← start here
  docs/superpowers/audits/2026-05-22/09_remediation_plan.md  ← then this

Approve Batch 1 to proceed with hardening PRs, or request edits to the plan.
```

- [ ] **Step 2: Wait for the user's explicit approval (or revision request)**

Do not proceed to Task 13 without one of:
- "Approve Batch 1" — proceed
- "Revise the plan because X" — edit `09_remediation_plan.md` and re-surface

---

## Task 13: Open Batch 1 remediation PR

Per the Definition of Done in the spec §7, the system isn't complete until one remediation batch ships. This task opens that PR.

- [ ] **Step 1: Create a new branch for Batch 1**

```bash
git checkout -b audit/batch-1-critical-high-small
```

- [ ] **Step 2: For each finding in Batch 1, apply the recommendation**

Work through Batch 1's table top-to-bottom. For each row:
1. Read the `Finding` and `Recommendation` columns.
2. Make the change in the referenced file.
3. Run `flutter analyze` after each change.
4. Commit with `fix(audit): <one-line finding>` referencing the row.

Do not batch unrelated changes into one commit — one commit per finding makes review tractable.

- [ ] **Step 3: Run full validation**

```bash
flutter analyze
flutter test patrol_test
```
Both must pass before opening the PR.

- [ ] **Step 4: Open the PR**

```bash
git push -u origin audit/batch-1-critical-high-small
gh pr create --title "fix(audit): Batch 1 — Critical/High findings (S effort)" --body "$(cat <<'EOF'
## Summary

First batch of remediations from the 2026-05-22 quality audit. Covers all Critical and High severity findings with Small effort (under 1 hour each).

See `docs/superpowers/audits/2026-05-22/09_remediation_plan.md` for the full batch list and `00_report.md` for the source findings.

## Test plan

- [ ] `flutter analyze` passes
- [ ] `flutter test patrol_test` passes
- [ ] Each commit references one finding row from the audit
- [ ] No new findings introduced (re-run `/audit-security` and `/audit-architecture` to verify)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 5: Return the PR URL to the user**

This closes the implementation loop. Subsequent batches (2, 3, 4) follow the same Task 13 pattern but are out of scope for this plan — track them in `09_remediation_plan.md`.

---

## Out of scope (deferred)

These appear in the spec but are explicitly NOT part of this plan:

- **CI integration of slash commands.** Slash commands run locally only.
- **Automated fixes during the audit pass.** All 8 agents are read-only.
- **Threat modeling phase.** Approach C was rejected during brainstorming.
- **GitHub issue per batch.** Spec §6.1 defaults to PR-only.
- **`/audit-deps` auto-upgrade.** Spec §6.2 defaults to strictly read-only.
- **Batches 2/3/4 PRs.** Out of scope for this plan; tracked in `09_remediation_plan.md` and opened ad-hoc.
