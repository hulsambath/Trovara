# Architecture Audit — 2026-05-22

Generated by `/audit`. Complements the `style-reviewer` subagent (feature-scoped) — this view is whole-codebase.

| Severity | Category | File:Line | Finding | Recommendation | Effort |
|---|---|---|---|---|---|
| Medium | FileSize | lib/views/search/search_content.dart:1 | 875 LOC exceeds 300 limit | Apply Recipe R1: extract filter panel, sort sheet, result cards, and empty state into `widgets/` part files | L |
| Medium | FileSize | lib/core/services/ai/rag_service.dart:1 | 681 LOC exceeds 300 limit | Apply Recipe R2: extract query-rewrite orchestration and source-attribution helpers into sibling files under `ai/` | L |
| Medium | FileSize | lib/core/services/ai/llm_client.dart:1 | 667 LOC exceeds 300 limit | Apply Recipe R5: one file per provider in `_providers/` (gemini, openai, openrouter); keep public API in `llm_client.dart` | L |
| Medium | FileSize | lib/widgets/tages/unified_tags_icon_button.dart:1 | 580 LOC exceeds 300 limit | Apply Recipe R1: extract tag-section widgets (MoodSection, ActivitySection, etc.) into `widgets/` part files | L |
| Medium | FileSize | lib/views/notes/note/note_view_model.dart:1 | 534 LOC exceeds 300 limit | Apply Recipe R3: extract save/sync/embed orchestration into `lib/core/services/notes/note_edit_service.dart`; keep only UI state in ViewModel | L |
| Medium | FileSize | lib/widgets/tages/custom/custom_tags_widget.dart:1 | 513 LOC exceeds 300 limit | Apply Recipe R1+R4: extract `_TagListItem`, `_TagCreateRow`, dialog widgets into part files | L |
| Medium | FileSize | lib/core/services/ai/embedding_service.dart:1 | 399 LOC exceeds 300 limit | Apply Recipe R2: extract chunking logic and SHA-256 diff helpers into `_internal/` sub-folder | M |
| Medium | FileSize | lib/views/setting/setting_content.dart:1 | 283 LOC near 300 limit | Apply Recipe R1: extract account section, backup section, and icon picker into `widgets/` part files | M |
| High | MVVM | lib/views/chat/widgets/chat_drawer.dart:23 | Widget `_ChatDrawer.build()` calls `ServiceLocator().chatService` directly, bypassing the ViewModel | Move thread-fetching logic into `ChatViewModel`; pass threads as a constructor argument to the drawer widget | M |
| High | MVVM | lib/views/notes/widgets/note_card.dart:87 | `_NoteCard` calls `TextParserService.parseQuillContent()` and `getPreviewText()` directly in the widget | Pre-compute preview text in `NotesViewModel` and pass as a field on the note display model | M |
| High | MVVM | lib/views/setting/setting_content.dart:89 | `_SettingContent` calls `AppIconService.isSupported`, `getCurrentIcon()`, and `changeIcon()` directly (imported via `setting_view.dart`) | Move all `AppIconService` calls into `SettingViewModel`; expose `isIconSupported`, `currentIcon`, and `changeIcon()` | M |
| High | MVVM | lib/views/search/search_content.dart:708 | `search_content.dart` calls `TextParserService.parseQuillContent()` directly inside a widget method | Move full-text extraction into `SearchViewModel`; expose a `previewFor(note)` getter | S |
| High | MVVM | lib/views/setting/setting_view_model.dart:222 | ViewModel uses `Navigator.of(context).push(MaterialPageRoute(...))` to open `TrashView` | Replace with `context.push('/trash')` via go_router (define the named route if missing) | S |
| Low | I18n | lib/views/setting/setting_content.dart:63 | 19 hardcoded English strings in `Text()` widgets (Sign in with Google, Sync, Back up, Restore, etc.) | Move all to `assets/translations/en.json` + `km.json` and call `tr('key')` | M |
| Low | I18n | lib/views/notes/notes_view_model.dart:125 | Hardcoded dialog strings: "Edit", "Delete", "Delete Note", "Are you sure…", confirmation messages | Extract to i18n keys under `notes.dialog.*` in both translation files | S |
| Low | I18n | lib/views/setting/advanced/advanced_setting_content.dart:20 | 9 hardcoded English strings (Export as Markdown, From Obsidian, Re-index all notes, etc.) | Move to `assets/translations/en.json` + `km.json` under `settings.advanced.*` | S |
| Low | I18n | lib/views/trash/trash_content.dart:10 | Hardcoded strings: "Recently Deleted", "Note restored", "Failed to restore", "Delete Forever", "Cancel", "Note permanently deleted" | Move to i18n keys under `trash.*` | S |
| Low | I18n | lib/views/chat/widgets/chat_drawer.dart:207 | Hardcoded dialog strings: "Delete conversation", "This conversation will be permanently deleted.", "Cancel", "Delete" | Move to i18n keys under `chat.dialog.*` | S |
| Low | I18n | lib/views/insights/insights_content.dart:10 | Hardcoded `'Insights'` app bar title and `'Retry'` button label | Move to i18n keys | S |
| Low | Color | lib/views/trash/trash_content.dart:54 | `Colors.green`, `Colors.white`, `Colors.red` hardcoded in swipe-action backgrounds and snackbars | Use `Theme.of(context).colorScheme.error` / `.primary` / `.onPrimary` | S |
| Low | Color | lib/views/notes/notes_view_model.dart:115 | `Colors.red` used 5× for favorite icon color, delete action, snackbar background | Use `Theme.of(context).colorScheme.error` | S |
| Low | Color | lib/views/search/search_content.dart:283 | `Colors.red` used for favorite filter chip `activeColor`; `Colors.red.withValues()` for inline favorite icon | Use `colorScheme.error` | S |
| Low | Icon | lib/views/insights/widgets/tag_display_helper.dart:49 | `Icons.local_activity_outlined`, `Icons.schedule_outlined`, `Icons.trending_up_outlined` used as fallback icons | Replace with Lucide equivalents: `LucideIcons.activity`, `LucideIcons.clock`, `LucideIcons.trendingUp` | S |
| Low | Icon | lib/views/insights/widgets/sentiment_trend_chart.dart:15 | `Icons.mood_outlined` used in `ChartEmptyState` | Replace with `LucideIcons.smile` | S |
| Low | Icon | lib/views/insights/widgets/chart_empty_state.dart:4 | `Icons.insights_outlined` as default parameter | Replace with a Lucide icon (e.g., `LucideIcons.chartColumn`) and update `ChartEmptyState` default | S |
| Low | Icon | lib/views/notes/notes_view_model.dart:114 | `Icons.favorite_rounded` mixed with `LucideIcons.heart` for the same toggle action | Standardize on `LucideIcons.heart` / `LucideIcons.heartFill` exclusively | S |
| Low | Icon | lib/views/notes/widgets/note_card.dart:73 | `Icons.favorite_rounded` used inline for favorite indicator | Replace with `LucideIcons.heartFill` | S |
| Low | Icon | lib/views/search/search_content.dart:669 | `Icons.favorite_rounded` inside a search result row | Replace with `LucideIcons.heartFill` | S |
| Low | Icon | lib/main.dart:33 | `Icons.error_outline` in the global error fallback widget | Replace with `LucideIcons.circleAlert` | S |
| Low | Print | lib/core/services/app/mock_data_service.dart:24 | Two `print()` calls (lines 24, 81) | Replace with `logger.d(...)` | S |
| Low | Print | lib/core/services/notes/note_merge_engine.dart:66 | Two `print()` calls (lines 66, 100) | Replace with `logger.d(...)` / `logger.w(...)` | S |
| Low | Print | lib/views/setting/setting_view_model.dart:90 | `print('Auto-sync after sign-in failed: $e')` behind `kDebugMode` | Replace with `_logger.w(...)` | S |
| Low | TextStyle | lib/main.dart:37 | `TextStyle(fontSize: 20, fontWeight: FontWeight.bold)` hardcoded in the global error fallback | Use `Theme.of(context).textTheme.titleLarge` | S |
| Low | TextStyle | lib/views/insights/insights_content.dart:79 | `TextStyle(color: scheme.onErrorContainer)` — uses theme color correctly but bypasses `textTheme`; inline style on Retry button | Use `TextButton.styleFrom(foregroundColor: scheme.onErrorContainer)` instead | S |
