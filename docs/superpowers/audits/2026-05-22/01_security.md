# Security Audit — 2026-05-22

Generated by `/audit`. See `00_report.md` for aggregated cross-dimension view.

| Severity | Category | File:Line | Finding | Recommendation | Effort |
|---|---|---|---|---|---|
| High | OAuth / Stale Auth | lib/core/services/auth/google_drive_service.dart:83 | `_ensureDriveApi()` snapshots `authHeaders` once at `DriveApi` construction time and caches `_driveApi` forever (`if (_driveApi != null) return`). Google OAuth tokens expire in ~1 hour; subsequent Drive calls silently use stale headers until a 401 forces `ensureAuthenticatedDriveApi()` to call `signIn()`, which triggers a full interactive re-auth without user explanation. | Invalidate `_driveApi` on sign-in and re-fetch auth headers before every long-running session, or wrap `_GoogleAuthClient.send()` to call `currentUser?.authHeaders` on each request so the Google Sign-In SDK can transparently refresh the token. | M |
| High | Ownership | lib/core/repository/implementations/objectbox_note_repository.dart:117 | `_userOwnershipCondition` matches both `userId == currentUser` AND `userId IS NULL`. After a user logs in and new notes are created, any anonymous (pre-login) note still in the store is visible to any Google account that signs in on the same device, including a different account. | On first sign-in, `_assignUserIdToAnonymousNotes` (sync service) should complete before any query is served; enforce that `userId IS NULL` match is disabled once a user has ever signed in (store a "ever signed in" flag and omit the `isNull()` arm). | M |
| High | Ownership | lib/core/services/ai/vector_search_service.dart:67 | `VectorSearchService.search()` calls `_repository.getAllEmbeddings()` (objectbox `box.getAll()`), which returns every embedding in the store with no userId filter. If multiple Google accounts ever signed in on the same device, each account's RAG responses can leak the other account's note content through shared embedding chunks. | Add a `userId` field to `NoteEmbedding` entity and filter in `getAllEmbeddings()` or in `VectorSearchService.search()` by the current user. | L |
| High | Ownership | lib/core/repository/implementations/objectbox_chat_thread_repository.dart:38 | `getAllThreads()`, `getGlobalThreads()`, and `getThreadsByNote()` do not filter by userId. Chat history from any user who has ever signed in on the device is visible to any subsequent user. | Add `userId` to `ChatThread` entity and scope all repository queries to the current user's ID. | L |
| Medium | Secrets — Plain-text storage | lib/core/storage/google_drive_auth_storage.dart:8 | `GoogleDriveAccountIdStorage` (and `EmailStorage`, `NameStorage`) extend `StringStorage → DefaultStorage → SharedPreferencesAdapter`, storing the Google account ID and email in unencrypted `SharedPreferences`. The account ID is also used as the `userId` ownership key for notes. On rooted Android devices this value is accessible without app permission. | Migrate auth identity fields to `flutter_secure_storage` (Keychain on iOS, Android Keystore-backed EncryptedSharedPreferences on Android). | M |
| Medium | Ownership | lib/core/services/notes/note_service.dart:202 | `NoteService.notes` (line 202) and `NoteService.deletedNotes` (line 204) call the unscoped `getActiveNotes()` / `getDeletedNotes()` repository methods. These are used in `advanced_setting_view_model.dart:53,185` for export and re-indexing, bypassing user scoping. A second logged-in account would export or re-embed another user's notes. | Replace with `notesForUser(userId)` / `deletedNotesForUser(userId)` in all callers in `advanced_setting_view_model.dart` and in `note_import_from_json.dart:96` and `note_import_from_adapter.dart:140`. | S |
| Medium | Prompt Injection | lib/core/services/ai/prompt_builder_service.dart:139 | User-typed chat queries and note content (titles, tags, full text) are concatenated directly into the LLM prompt via string interpolation without any escaping or sanitization. A crafted note title like `"Ignore previous instructions and..."` can redirect the LLM. Note content imported from untrusted Obsidian/Notion exports flows through the same path. | Add a sanitization pass that strips or escapes prompt-control phrases (e.g. "ignore previous instructions", "system:", "assistant:") from note metadata before injecting into the prompt context block. Consider wrapping injected content in XML-style delimiters the system prompt instructs the model to treat as data, not instructions. | L |
| Medium | Prompt Injection | lib/core/import/adapters/notion_adapter.dart:210 | `_cleanNotionBody()` strips HTML tags with `replaceAll(RegExp(r'<[^>]+>'), '')` but does not strip `<script>`, event handlers (`onload=`, `onerror=`), or prompt-injection strings before the body is stored in ObjectBox and later injected into LLM prompts. Notion exports can legitimately embed raw HTML callouts. | Sanitize imported markdown bodies through an allow-list Markdown parser that rejects embedded HTML before the content reaches ObjectBox. | M |
| Medium | Drive OAuth — Scope | lib/core/services/auth/google_drive_service.dart:20 | The requested scopes include `'email'` and `'profile'` in addition to `driveAppdataScope`. These two extra scopes are passed to Google Sign-In but are never consumed by the app (account metadata is read from `GoogleSignInAccount`, which does not require explicit scope grants for basic profile). Requesting unnecessary scopes widens the OAuth consent surface shown to users. | Remove `'email'` and `'profile'` from the `GoogleSignIn` scopes list; the app does not make any API calls requiring them. | S |
| Low | Secrets — Logging | lib/core/services/ai/llm_client.dart:125 | `_logger.i('LlmClient initialized (OpenAI-compatible) (baseUrl=$_baseUrl, model=$_modelName)')` logs the `baseUrl` to the logger at Info level. While not the API key itself, logging the endpoint in production may assist attackers in identifying the backend. | Emit this log only in debug mode (wrap in `assert()` or check `kDebugMode`). | S |
| Low | Secrets — Default fallback | lib/constants/config_constants.dart:44 | `OPENROUTER_MODEL` defaults to `'openrouter/auto'` and `OPENROUTER_EMBEDDING_MODEL` to `'openai/text-embedding-3-large'` when `--dart-define` is absent. A release build compiled without `--dart-define=OPENROUTER_MODEL=...` silently falls back to these values and will make real API calls (and incur costs) if `OPENROUTER_API_KEY` is set. | Document that all three OpenRouter keys must be set together in CI/CD; add a runtime assertion in `ServiceLocator.initialize()` that throws if `openRouterApiKey` is non-empty but `openRouterModel` is the default sentinel value. | S |
